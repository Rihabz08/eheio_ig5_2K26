{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Hello Home!{% endblock %}

{% block body %}

    <div class="example-wrapper">
        <h1>{{ text }}</h1>

        <ul>
            {% for year in years %}
                <li>{{ year }}</li>
            {% endfor %}
        </ul>
        
    </div>
{% endblock %}